Given the following query and a set of active memory units (established facts), rate how relevant each memory unit is to the query on a scale of 0.0 to 1.0.

A score of 1.0 means the memory unit is directly relevant to answering or contextualizing the query.
A score of 0.0 means the memory unit is completely irrelevant to the query.

Query: {{ query }}

Memory units:
{% for unit in units %}- ID: {{ unit.id }} | Text: {{ unit.text }}
{% endfor %}

Respond with ONLY valid JSON, no markdown, no explanation:
{"scores": {"<unit_id>": <relevance_score>, ...}}
