## 1. Update Documentation

- [x] 1.1 In `CLAUDE.md` Key Design Decisions #15, replace `T0_INVARIANT`, `T1_WORKING`, `T2_EPISODIC` with `COLD`, `WARM`, `HOT`
- [x] 1.2 In `openspec/project.md` Key Design Principles section, replace the same stale T-prefixed names with `COLD`, `WARM`, `HOT`

## 2. Verify

- [x] 2.1 Grep the repository for `T0_INVARIANT`, `T1_WORKING`, `T2_EPISODIC` — no matches should remain outside of the archived change directory
